/**
 * useCart.js — Cart helper hooks.
 * 
 * All cart state is local (Zustand + localStorage).
 * No API calls needed — cart works without authentication.
 */
export { useCartStore } from '../store/cartStore';
